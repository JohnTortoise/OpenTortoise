version: '3.7'

services:
  tortoise-admin:
    build: .
    image: tortoise-admin:v1.0.0
    container_name: tortoise-admin
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env